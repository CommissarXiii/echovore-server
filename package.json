{
  "name": "echovore-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd dist && node ./bin/www",
    "watch": "babel-watch src/bin/www",
    "build": "./build.sh"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-watch": "^2.0.7"
  }
}
